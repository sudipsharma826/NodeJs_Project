<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= blog ? blog.title : 'No Blog Found' %></title>
    <link rel="icon" href="https://res.cloudinary.com/diuy2xilf/image/upload/v1727609897/Screenshot_2024-05-18_163234-removebg-preview_l3x0bf.png" type="image/png">
    <link rel="stylesheet" href="/singleBlog.css"> <!-- Link to the scoped blog CSS -->
</head>
<body>
    <%- include('./message.ejs', { success: success, error: error }) %>


    <!-- Navbar Section -->
 <%- include ('./navbar.ejs') %> 

    <!-- Blog Page Content -->
    <div class="blog-page">
        <div class="blog-card">
            <div class="blog-image">
                <img src="<%= blog.image %>" alt="<%=blog.image%>">
            </div>
            <div class="blog-content">
                <h1 class="blog-title"><%= blog.title %></h1>
                <h3 class="blog-subtitle"><%= blog.subtitle %></h3>
                <p class="blog-description"><%= blog.description %></p>
                <p>Written by: <%= blog.register.username %></p>
                <p>Contact: <%= blog.register.email %></p>
            </div>
            <div class="blog-actions">
                <% if (isLoggedIn && currentuserId === blog.register.id) { %>
                    <a href="/edit/<%= blog.id %>" class="edit-button">Edit</a>
                    <a href="/delete/<%= blog.id %>/<%= blog.title %>" class="delete-button">Delete</a>

                <% } %>
                
            </div>
        </div>
    </div>

</body>
</html>
